Który komponent Spring jest odpowiedzialny za wstrzykiwanie zależności?;@Autowired;@Inject;@Component;@Bean;@Autowired
Który plik konfiguracyjny jest najczęściej używany w aplikacjach Spring do definiowania ziaren (beans) i konfiguracji aplikacji?;applicationContext.xml;spring-config.properties;beans.properties;config.xml;applicationContext.xml
Jakie jest zadanie Spring Boot Starter Parent w projekcie Spring Boot?;Tworzenie rodzica dla wszystkich klas kontrolerów;Zapewnienie ustawień domyślnych i konfiguracji dla projektu Spring Boot;Udostępnienie gotowych szablonów HTML dla interfejsu użytkownika;Umożliwienie wyboru wariantów frameworka Spring podczas tworzenia projektu;Zapewnienie ustawień domyślnych i konfiguracji dla projektu Spring Boot
Która adnotacja w Spring jest używana do oznaczania klasy jako komponentu, który może być automatycznie skanowany i zarządzany przez kontener Springa?;@Service;@Repository;@Component;@Controller;@Component
Jakie są główne cechy aspektów (aspects) w Spring AOP?;Umożliwiają dodawanie nowych funkcjonalności do istniejącego kodu bez modyfikacji samego kodu;Pozwalają na wyodrębnienie funkcjonalności wspólnej dla wielu klas;Są stosowane głównie w warstwie prezentacji aplikacji;Pozwalają na manipulację zasobami w aplikacji;Umożliwiają dodawanie nowych funkcjonalności do istniejącego kodu bez modyfikacji samego kodu
Która z poniższych opcji opisuje poprawnie wstrzykiwanie zależności przez konstruktor w Spring?;Wstrzykiwanie zależności przez konstruktor jest opcjonalne w Spring;Wstrzykiwanie zależności przez konstruktor jest zalecane ze względu na unikanie stanu niezainicjowanego obiektu;Wstrzykiwanie zależności przez konstruktor jest wykonywane za pomocą adnotacji @Inject;Wstrzykiwanie zależności przez konstruktor jest wykonywane tylko za pomocą adnotacji @Autowired;Wstrzykiwanie zależności przez konstruktor jest zalecane ze względu na unikanie stanu niezainicjowanego obiektu
Które z poniższych są przykładami komponentów Springa?;@Service;@Component;@Model;@Repository;@Service,@Component,@Repository
Która adnotacja w Spring jest używana do oznaczania metody jako punktu wejścia (entry point) dla żądań HTTP w warstwie kontrolerów?;@RequestMapping;@ControllerAdvice;@GetMapping;@RestController;@GetMapping
Który mechanizm w Spring Security jest odpowiedzialny za uwierzytelnianie użytkowników?;AuthenticationProvider;UserDetailsManager;AuthenticationManager;UserDetailsService;AuthenticationManager
Które z poniższych zastosowań jest głównym celem użycia modułu Spring Data JPA?;Automatyczne generowanie kodu HTML na podstawie modelu danych;Ułatwienie interakcji z bazą danych przy użyciu JPA w aplikacjach Spring;Tworzenie interfejsów użytkownika w aplikacjach webowych;Zapewnienie bezpieczeństwa w aplikacjach Spring;Ułatwienie interakcji z bazą danych przy użyciu JPA w aplikacjach Spring